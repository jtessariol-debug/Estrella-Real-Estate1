-- Datos demo opcionales. Ejecuta este archivo después de la migración.
-- Las propiedades se omiten deliberadamente: deben crearse con imágenes reales desde el panel.
-- Las amenidades iniciales ya se incluyen en la migración principal.

-- Primer administrador (ejecutar después de crear el usuario en Authentication > Users):
-- insert into public.profiles (id, full_name, role)
-- values ('USER_UUID_HERE', 'Nombre del administrador', 'admin');
